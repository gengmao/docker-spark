#!/bin/sh

cd /usr/local/spark

export SPARK_MASTER_IP=`awk 'NR==1 {print $1}' /etc/hosts`
export SPARK_LOCAL_IP=`awk 'NR==1 {print $1}' /etc/hosts`

./bin/spark-class org.apache.spark.deploy.master.Master -i $SPARK_LOCAL_IP
